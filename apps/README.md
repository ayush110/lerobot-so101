# Applications

Human-facing surfaces belong here: an operator CLI, calibration UI, telemetry
dashboard, dataset inspector, and experiment dashboard. Applications compose
shared packages; they do not become shared dependency libraries themselves.
