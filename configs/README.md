# Configuration

Portable defaults and examples are committed here. Host-specific serial ports,
camera device identifiers, credentials, and active calibration references go
in `configs/local/`, which is ignored.

All configuration files begin with `schema_version`. Training and evaluation
runs must save their fully resolved configuration with the result.
