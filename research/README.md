# Research workflow

Each study gets a directory in `experiments/<study-id>/` containing a question,
predeclared metrics, fixed evaluation protocol, configuration, results, and a
short conclusion. Generated checkpoints and large media remain outside Git.

Use architecture decision records in `docs/adr/` for durable engineering
choices. Use this directory for empirical claims and ablations.
