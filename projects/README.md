# Projects

A project is an end-to-end robot capability with a task definition, acceptance
metrics, configuration, and demo. It may compose shared packages, but should
not expose itself as a dependency of another project.

Start experiments inside a project. Promote only genuinely reusable code into
the shared layers after a second consumer appears.
